services:
  api:
    build: .
    container_name: agent-manager-api
    ports:
      - "8080:8080"
    environment:
      DATABASE_URL: jdbc:sqlite:data/agent-manager.db
      SERVER_PORT: 8080
    volumes:
      - ./data:/app/data
